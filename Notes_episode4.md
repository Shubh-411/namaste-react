# React Fragments: It let you group a list of children without adding extra nodes to the DOM. It is a component which is imported by React. JSX can have only one parent element.

# Config Driven UI : Same website work different for different locations. So, here we control our frontend using config(backend drives config).

# Optional Chaining:
